{
  "name": "ayooda",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,widget -c blue,green,magenta \"pnpm dev:api\" \"pnpm dev:web\" \"pnpm dev:widget\"",
    "dev:web": "pnpm --filter web dev",
    "dev:api": "pnpm --filter api dev",
    "dev:login": "pnpm --filter web dev:login",
    "dev:widget": "pnpm --filter widget dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "kb:validate": "node scripts/validate-knowledge-base.mjs",
    "kb:import": "pnpm --filter api kb:import"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "typescript": "^5.4.5"
  }
}
